<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prosperousitsolutions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      
        <activity android:name=".SplashActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
        </activity>
      
        <activity android:name=".IntroActivity"/>
        <activity android:name=".user_session.Forgot_PasswordActivity"></activity>
        <activity android:name=".user_session.OtpActivity" />
        <activity
            android:name=".user_session.RegisterActivity"
            android:launchMode="singleInstance">
        </activity>

        <activity
            android:name=".user_session.LoginActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>